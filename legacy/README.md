# Legacy Next.js prototype

The app was migrated to **Vite + React Router** (`packages/web`) and **Fastify** (`packages/api`).

`next.config.js` is kept here for reference only; the active stack is documented in `docs/technical-reference.md`.
